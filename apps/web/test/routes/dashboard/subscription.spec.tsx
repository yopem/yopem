import { describe, expect, test } from "vite-plus/test"

import { Route } from "@/routes/dashboard/subscription"

describe("Dashboard Subscription Route", () => {
  test("is defined", () => {
    expect(Route).toBeDefined()
  })
})
