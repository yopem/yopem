import { describe, expect, test } from "vite-plus/test"

import { TagDialog } from "@/components/categories-tags/tag-dialog"

describe("TagDialog", () => {
  test("is a React component", () => {
    expect(typeof TagDialog).toBe("function")
  })
})
