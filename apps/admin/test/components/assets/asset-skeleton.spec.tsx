import { describe, expect, test } from "vite-plus/test"

import { AssetSkeleton } from "@/components/assets/asset-skeleton"

describe("AssetSkeleton", () => {
  test("is a React component", () => {
    expect(typeof AssetSkeleton).toBe("function")
  })
})
