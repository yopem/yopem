{
  "name": "yopem",
  "private": true,
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/yopem/yopem.git"
  },
  "author": "Karyana Yandi <karyana@yandi.me>",
  "license": "AGPL-3.0-or-later",
  "scripts": {
    "build": "turbo build",
    "build:api": "turbo -F @yopem/api build",
    "build:web": "turbo -F @yopem/web build",
    "check-types": "turbo check-types",
    "clean": "pnpm -r --parallel exec git clean -xdf dist .cache .turbo .next *.log",
    "clean:all": "pnpm clean && pnpm clean:deps",
    "clean:deps": "pnpm -r --parallel exec git clean -xdf node_modules",
    "db:generate": "turbo -F api db:generate",
    "db:migrate": "turbo -F api db:migrate",
    "db:push": "turbo -F api db:push",
    "db:studio": "turbo -F api db:studio",
    "dev": "turbo dev",
    "dev:api": "turbo -F @yopem/api dev",
    "dev:mobile": "turbo -F @yopem/mobile dev",
    "dev:web": "turbo -F @yopem/web dev",
    "format:check": "turbo format:check",
    "format:write": "turbo format:write",
    "lint": "turbo lint",
    "lint:fix": "turbo lint:fix",
    "typecheck": "turbo typecheck"
  },
  "devDependencies": {
    "@yopem/eslint-config": "catalog:config",
    "@yopem/prettier-config": "catalog:config",
    "lefthook": "1.11.12",
    "prettier": "catalog:config",
    "turbo": "2.5.3",
    "typescript": "catalog:config"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@10.11.0",
  "prettier": "@yopem/prettier-config/base"
}
