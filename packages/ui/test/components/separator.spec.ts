import { describe, expect, test } from "vite-plus/test"

import { Separator } from "ui/components/separator"

describe("separator", () => {
  test("Separator is exported", () => {
    expect(Separator).toBeDefined()
  })
})
