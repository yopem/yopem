import { describe, expect, test } from "vitest"

import { Checkbox } from "ui/components/checkbox"

describe("checkbox", () => {
  test("Checkbox is exported", () => {
    expect(Checkbox).toBeDefined()
  })
})
