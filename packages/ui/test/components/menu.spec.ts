import { describe, expect, test } from "vite-plus/test"

import {
  MenuCreateHandle,
  DropdownMenuCreateHandle,
  Menu,
  DropdownMenu,
  MenuPortal,
  DropdownMenuPortal,
  MenuTrigger,
  DropdownMenuTrigger,
  MenuPopup,
  DropdownMenuContent,
  MenuGroup,
  DropdownMenuGroup,
  MenuItem,
  DropdownMenuItem,
  MenuCheckboxItem,
  DropdownMenuCheckboxItem,
  MenuRadioGroup,
  DropdownMenuRadioGroup,
  MenuRadioItem,
  DropdownMenuRadioItem,
  MenuGroupLabel,
  DropdownMenuLabel,
  MenuSeparator,
  DropdownMenuSeparator,
  MenuShortcut,
  DropdownMenuShortcut,
  MenuSub,
  DropdownMenuSub,
  MenuSubTrigger,
  DropdownMenuSubTrigger,
  MenuSubPopup,
  DropdownMenuSubContent,
} from "ui/components/menu"

describe("menu", () => {
  test("MenuCreateHandle is exported", () => {
    expect(MenuCreateHandle).toBeDefined()
  })

  test("DropdownMenuCreateHandle is exported", () => {
    expect(DropdownMenuCreateHandle).toBeDefined()
  })

  test("Menu is exported", () => {
    expect(Menu).toBeDefined()
  })

  test("DropdownMenu is exported", () => {
    expect(DropdownMenu).toBeDefined()
  })

  test("MenuPortal is exported", () => {
    expect(MenuPortal).toBeDefined()
  })

  test("DropdownMenuPortal is exported", () => {
    expect(DropdownMenuPortal).toBeDefined()
  })

  test("MenuTrigger is exported", () => {
    expect(MenuTrigger).toBeDefined()
  })

  test("DropdownMenuTrigger is exported", () => {
    expect(DropdownMenuTrigger).toBeDefined()
  })

  test("MenuPopup is exported", () => {
    expect(MenuPopup).toBeDefined()
  })

  test("DropdownMenuContent is exported", () => {
    expect(DropdownMenuContent).toBeDefined()
  })

  test("MenuGroup is exported", () => {
    expect(MenuGroup).toBeDefined()
  })

  test("DropdownMenuGroup is exported", () => {
    expect(DropdownMenuGroup).toBeDefined()
  })

  test("MenuItem is exported", () => {
    expect(MenuItem).toBeDefined()
  })

  test("DropdownMenuItem is exported", () => {
    expect(DropdownMenuItem).toBeDefined()
  })

  test("MenuCheckboxItem is exported", () => {
    expect(MenuCheckboxItem).toBeDefined()
  })

  test("DropdownMenuCheckboxItem is exported", () => {
    expect(DropdownMenuCheckboxItem).toBeDefined()
  })

  test("MenuRadioGroup is exported", () => {
    expect(MenuRadioGroup).toBeDefined()
  })

  test("DropdownMenuRadioGroup is exported", () => {
    expect(DropdownMenuRadioGroup).toBeDefined()
  })

  test("MenuRadioItem is exported", () => {
    expect(MenuRadioItem).toBeDefined()
  })

  test("DropdownMenuRadioItem is exported", () => {
    expect(DropdownMenuRadioItem).toBeDefined()
  })

  test("MenuGroupLabel is exported", () => {
    expect(MenuGroupLabel).toBeDefined()
  })

  test("DropdownMenuLabel is exported", () => {
    expect(DropdownMenuLabel).toBeDefined()
  })

  test("MenuSeparator is exported", () => {
    expect(MenuSeparator).toBeDefined()
  })

  test("DropdownMenuSeparator is exported", () => {
    expect(DropdownMenuSeparator).toBeDefined()
  })

  test("MenuShortcut is exported", () => {
    expect(MenuShortcut).toBeDefined()
  })

  test("DropdownMenuShortcut is exported", () => {
    expect(DropdownMenuShortcut).toBeDefined()
  })

  test("MenuSub is exported", () => {
    expect(MenuSub).toBeDefined()
  })

  test("DropdownMenuSub is exported", () => {
    expect(DropdownMenuSub).toBeDefined()
  })

  test("MenuSubTrigger is exported", () => {
    expect(MenuSubTrigger).toBeDefined()
  })

  test("DropdownMenuSubTrigger is exported", () => {
    expect(DropdownMenuSubTrigger).toBeDefined()
  })

  test("MenuSubPopup is exported", () => {
    expect(MenuSubPopup).toBeDefined()
  })

  test("DropdownMenuSubContent is exported", () => {
    expect(DropdownMenuSubContent).toBeDefined()
  })
})
