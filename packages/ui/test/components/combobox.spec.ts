import { describe, expect, test } from "vite-plus/test"

import {
  Combobox,
  ComboboxInput,
  ComboboxTrigger,
  ComboboxPopup,
  ComboboxItem,
  ComboboxSeparator,
  ComboboxGroup,
  ComboboxGroupLabel,
  ComboboxEmpty,
  ComboboxValue,
  ComboboxList,
  ComboboxClear,
  ComboboxStatus,
  ComboboxRow,
  ComboboxCollection,
  ComboboxChips,
  ComboboxChip,
  useComboboxFilter,
} from "ui/components/combobox"

describe("combobox", () => {
  test("Combobox is exported", () => {
    expect(Combobox).toBeDefined()
  })

  test("ComboboxInput is exported", () => {
    expect(ComboboxInput).toBeDefined()
  })

  test("ComboboxTrigger is exported", () => {
    expect(ComboboxTrigger).toBeDefined()
  })

  test("ComboboxPopup is exported", () => {
    expect(ComboboxPopup).toBeDefined()
  })

  test("ComboboxItem is exported", () => {
    expect(ComboboxItem).toBeDefined()
  })

  test("ComboboxSeparator is exported", () => {
    expect(ComboboxSeparator).toBeDefined()
  })

  test("ComboboxGroup is exported", () => {
    expect(ComboboxGroup).toBeDefined()
  })

  test("ComboboxGroupLabel is exported", () => {
    expect(ComboboxGroupLabel).toBeDefined()
  })

  test("ComboboxEmpty is exported", () => {
    expect(ComboboxEmpty).toBeDefined()
  })

  test("ComboboxValue is exported", () => {
    expect(ComboboxValue).toBeDefined()
  })

  test("ComboboxList is exported", () => {
    expect(ComboboxList).toBeDefined()
  })

  test("ComboboxClear is exported", () => {
    expect(ComboboxClear).toBeDefined()
  })

  test("ComboboxStatus is exported", () => {
    expect(ComboboxStatus).toBeDefined()
  })

  test("ComboboxRow is exported", () => {
    expect(ComboboxRow).toBeDefined()
  })

  test("ComboboxCollection is exported", () => {
    expect(ComboboxCollection).toBeDefined()
  })

  test("ComboboxChips is exported", () => {
    expect(ComboboxChips).toBeDefined()
  })

  test("ComboboxChip is exported", () => {
    expect(ComboboxChip).toBeDefined()
  })

  test("useComboboxFilter is exported", () => {
    expect(useComboboxFilter).toBeDefined()
  })
})
