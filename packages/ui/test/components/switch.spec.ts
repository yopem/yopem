import { describe, expect, test } from "vitest"

import { Switch } from "ui/components/switch"

describe("switch", () => {
  test("Switch is exported", () => {
    expect(Switch).toBeDefined()
  })
})
