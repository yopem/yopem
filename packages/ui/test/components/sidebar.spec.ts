import { describe, expect, test } from "vite-plus/test"

import {
  Sidebar,
  SidebarContent,
  SidebarFooter,
  SidebarGroup,
  SidebarGroupAction,
  SidebarGroupContent,
  SidebarGroupLabel,
  SidebarHeader,
  SidebarInput,
  SidebarInset,
  SidebarMenu,
  SidebarMenuAction,
  SidebarMenuBadge,
  SidebarMenuButton,
  SidebarMenuItem,
  SidebarMenuSkeleton,
  SidebarMenuSub,
  SidebarMenuSubButton,
  SidebarMenuSubItem,
  SidebarProvider,
  SidebarRail,
  SidebarSeparator,
  SidebarTrigger,
  useSidebar,
} from "ui/components/sidebar"

describe("sidebar", () => {
  test("Sidebar is exported", () => {
    expect(Sidebar).toBeDefined()
  })

  test("SidebarContent is exported", () => {
    expect(SidebarContent).toBeDefined()
  })

  test("SidebarFooter is exported", () => {
    expect(SidebarFooter).toBeDefined()
  })

  test("SidebarGroup is exported", () => {
    expect(SidebarGroup).toBeDefined()
  })

  test("SidebarGroupAction is exported", () => {
    expect(SidebarGroupAction).toBeDefined()
  })

  test("SidebarGroupContent is exported", () => {
    expect(SidebarGroupContent).toBeDefined()
  })

  test("SidebarGroupLabel is exported", () => {
    expect(SidebarGroupLabel).toBeDefined()
  })

  test("SidebarHeader is exported", () => {
    expect(SidebarHeader).toBeDefined()
  })

  test("SidebarInput is exported", () => {
    expect(SidebarInput).toBeDefined()
  })

  test("SidebarInset is exported", () => {
    expect(SidebarInset).toBeDefined()
  })

  test("SidebarMenu is exported", () => {
    expect(SidebarMenu).toBeDefined()
  })

  test("SidebarMenuAction is exported", () => {
    expect(SidebarMenuAction).toBeDefined()
  })

  test("SidebarMenuBadge is exported", () => {
    expect(SidebarMenuBadge).toBeDefined()
  })

  test("SidebarMenuButton is exported", () => {
    expect(SidebarMenuButton).toBeDefined()
  })

  test("SidebarMenuItem is exported", () => {
    expect(SidebarMenuItem).toBeDefined()
  })

  test("SidebarMenuSkeleton is exported", () => {
    expect(SidebarMenuSkeleton).toBeDefined()
  })

  test("SidebarMenuSub is exported", () => {
    expect(SidebarMenuSub).toBeDefined()
  })

  test("SidebarMenuSubButton is exported", () => {
    expect(SidebarMenuSubButton).toBeDefined()
  })

  test("SidebarMenuSubItem is exported", () => {
    expect(SidebarMenuSubItem).toBeDefined()
  })

  test("SidebarProvider is exported", () => {
    expect(SidebarProvider).toBeDefined()
  })

  test("SidebarRail is exported", () => {
    expect(SidebarRail).toBeDefined()
  })

  test("SidebarSeparator is exported", () => {
    expect(SidebarSeparator).toBeDefined()
  })

  test("SidebarTrigger is exported", () => {
    expect(SidebarTrigger).toBeDefined()
  })

  test("useSidebar is exported", () => {
    expect(useSidebar).toBeDefined()
  })
})
